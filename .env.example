# ==================== BloodOra - Environment Configuration ====================
# Copy to .env and fill values

PORT=3000
NODE_ENV=development
SESSION_SECRET=blood-donation-secret-key-2026-change-in-production

# Turso Serverless Database (Recommended for Production - Vercel/Serverless)
# Get from https://turso.tech -> Create Database
TURSO_DATABASE_URL=libsql://your-db-yourname.turso.io
TURSO_AUTH_TOKEN=your_turso_auth_token_here

# Fallback: Local SQLite file (works without Turso - for development)
# If TURSO vars are empty, app will fallback to file:./data/blood_network.db
# No extra config needed for local dev!

# Optional: File upload limit
MAX_UPLOAD_SIZE=16777216
